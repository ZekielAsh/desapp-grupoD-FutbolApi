services:
  postgres:
    image: postgres:15
    container_name: postgres-db
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: futbolapi   # 👈 usa minúsculas
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
